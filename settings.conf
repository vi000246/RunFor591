{
 # 程式執行區間
	"TimerIntervalSec" : 1,
    "TimerIntervalMinute" : 0,
    # json bin的key,用來儲存符合的物件
    "JsonBinKey":"",
    "SearchModel":{
      # 租屋類型 1:整層住家 2:獨立套房 3:分租套房 4:雅房 8:車位
      "kind":["1","2","3"],
      # 區域
      "Region":"1",
      # 鄉鎮 可選多個區域 例如"7,4"
      "Section":"",
      # 租金範圍 如果N以上，用"N,"  如果是A到B之間 用"A,B"
      "RendPrice":",13500",
      # 坪數 如果N以上，用"N,"  如果是A到B之間 用"A,B"
      "Area":"13,",
      # 排序
      "Order":"posttime",
      # 過濾是否有房屋照片 空值:不限
      "HasImg":"1",
      #是否為樓主刊登
      "Role":"",
      # 房屋類型 - `1`：公寓、`2`：電梯大樓、`3`：透天厝、`4`：別墅
      "Pattern":"",
      # 樓層
      "Floor":"",
      # 提供設備 - `tv`：電視、`cold`：冷氣、`icebox`：冰箱、`hotwater`：熱水器、`naturalgas`：天然瓦斯、
      # `four`：第四台、`broadband`：網路、`washer`：洗衣機、`bed`：床、`wardrobe`：衣櫃、`sofa`：沙發。可選擇多個設備，例如：option=tv,cold
      "Option":"pet",
      "FirstRow":""
    }
}