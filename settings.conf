{
    # 程式執行區間
	"TimerIntervalSec" : 1,
    "TimerIntervalMinute" : 0,
    # json hosting的url位置,用來儲存符合的物件
    "JsonHostingUrlPath":"",
    "SearchModel":{
      # 租屋類型 1:整層住家 2:獨立套房 3:分租套房 4:雅房 8:車位 (可多選)
      "kind":["1","2","3"],
      # 區域
      "Region":"1",
      # 鄉鎮 可選多個區域 例如"7,4" 或是不選
      "Section":"",
      # 租金範圍 如果N以上，用"N,"  如果是A到B之間 用"A,B"
      "RendPrice":",13500",
      # 坪數 如果N以上，用"N,"  如果是A到B之間 用"A,B" 0,0為不限
      "Area":"13,",
      # 排序要用的欄位 
      "Order":"posttime",
      # 排序的順序
      "OrderType":"desc",
      # 過濾是否有房屋照片 - `1`:是 空值:不限
      "HasImg":"1",
      #是否為樓主刊登
      "Role":"",
      # 房屋類型 - `1`：公寓、`2`：電梯大樓、`3`：透天厝、`4`：別墅
      "Pattern":"",
      # 是否為頂加 - `1` :是 空值:不限
      "NotCover":"",
      # 樓層  `0,0`：不限、`0,1`：一樓、`2,6`：二樓到六樓、`6,12`：六樓到十二樓、`12,`：十二樓以上
      "Floor":"",
      # 提供設備 - `tv`：電視、`cold`：冷氣、`icebox`：冰箱、`hotwater`：熱水器、`naturalgas`：天然瓦斯、
      # `four`：第四台、`broadband`：網路、`washer`：洗衣機、`bed`：床、`wardrobe`：衣櫃、`sofa`：沙發。可選擇多個設備，例如：option=tv,cold
      "Option":"pet",
      "FirstRow":""
    }
}